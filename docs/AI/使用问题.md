##### 飞书增加新的会话

```shell
直接在配置文件增加新的会话id,不一定能用
还需要去 C:\Users\用户名\.openclaw\credentials 目录,
有个feishu-allowFrom.json,在里面也增加会话id,
可能是这个文件的优先级大于配置文件导致
```

##### 因为代理问题连不上飞书

```shell
1 遇到使用代理之后,网关连不上飞书,执行以下命令重试
	set HTTP_PROXY=
    set HTTPS_PROXY=
    set http_proxy=
    set https_proxy=
    set NO_PROXY=
```



